import { PokemonSimpliste } from './pokemon-simpliste';

describe('PokemonSimpliste', () => {
  it('should create an instance', () => {
    expect(new PokemonSimpliste()).toBeTruthy();
  });
});
